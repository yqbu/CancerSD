device: "cuda"
seed: 42

paths:
  project_root: null
  data:
    root: "data"
    raw: "data/raw"
    processed: "data/processed"
  output:
    root: "outputs"
  log:
    root: "logs"

runtime:
  num_workers: 4
  pin_memory: true
  drop_last: true

logging:
  level: "INFO"